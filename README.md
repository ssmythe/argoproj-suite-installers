# argoproj-suite-installers
A set of scripts to install and cleanup the ArgoProj suite of tools on a local kubernetes cluster

# Requirements
* WSL2
* Docker Desktop with Kubernetes feature turned on
